'use strict';

/**
* If outgoing object contains _id, it is replaced with a property called id with the same value.  _id is removed
* @param  {Object} outgoingObject object to be transformed
* @return {Object}                a copy of the object with the 
*/
module.exports = () => {

};